@using static EscapeFromTarkov.Controllers.HomeController;
@{
    Layout = "_Layout";
    ViewData["Title"] = "Данные о НПС";
}
@model PrivateAccViewModel
<section>
    <form asp-action="AdminPanelNPS" method="get">
        <div class="grid-main">
            <div class="grid-container-main">
                <div><a asp-action="AdminPanelBoss">Боссы</a></div>
                <div><a asp-action="AdminPanelCard">Карты</a></div>
                <div><a asp-action="AdminPanelЦуфзщт">Оружие</a></div>
                <div><a asp-action="AdminPanelNPS">НПС</a></div>
                <div><a asp-action="AdminPanelProduct">Товары</a></div>
                <div><a asp-controller="swagger" asp-action="index.html">Swagger</a></div>
            </div>
            <div class="main-container">
                <table class="boss-table">
                    <tr>
                        <th colspan="3" style="font-size:25px;">НПС</th>
                    </tr>
                    <tr>
                        <th сolspan="1">НПСId</th>
                        <th colspan="1">Наименование</th>
                        <th colspan="1">Описание</th>
                        @*<th>Изображение</th>*@
                    </tr>
                    @foreach (var boss in Model.NPS)
                    {
                        <tr>
                            <td>@boss.ПерсонажиId</td>
                            <td>@boss.Наименование</td>
                            <td>@boss.Описание</td>
                            @*<td><img src="data:image/jpeg;base64,@Convert.ToBase64String(@boss.Изображение)" /></td>*@
                        </tr>
                    }
                </table>
            </div>
        </div>
    </form>
</section>